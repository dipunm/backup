sudo cp -r $recipe_data/preferences.d /etc/apt
sudo cp -r $recipe_data/sources.list.d /etc/apt
sudo cp -r $recipe_data/trusted.gpg.d /etc/apt
sudo cp $recipe_data/sources.list /etc/apt
sudo cp $recipe_data/trusted.gpg /etc/apt
