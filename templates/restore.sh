# $DIR_TMP:     A randomly generated directory which will be deleted after this script ends.
#               Use to store temporary files.
# $DIR_STORE:   A private directory to store any files that should be included in the backups.
# $DIR_RECIPE:  The path to the folder this script resides in.

# IMPORTANT: Be careful to exit with an appropriate exit code.